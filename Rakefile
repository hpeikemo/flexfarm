# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/flexfarm.rb'

Hoe.plugin :git

Hoe.spec('flexfarm') do
  # p.rubyforge_name = 'flexfarmx' # if different than lowercase project name
  developer('Hans Petter Eikemo', 'hpeikemo@me.com')
end

# vim: syntax=Ruby
